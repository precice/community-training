#!/bin/bash
cd ${0%/*} || exit 1    		    		# Run from this directory
. $WM_PROJECT_DIR/bin/tools/RunFunctions    # Tutorial run functions

casename="Fluid"

cd $casename
rm -rfv 0
cp -r 0.3ms_l_800k 0
cd ..

echo "change Dictionary for U"
changeDictionary -case ${casename} 2>&1 | tee log.changeDict

# Run
decomposePar -force -case ${casename}
#    mpirun -np $procs $solver -parallel -case ${casename} 2>&1 | tee log.pimpleFoam
#    reconstructPar -case ${casename}

